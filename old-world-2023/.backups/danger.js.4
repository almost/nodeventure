const exec = require('child_process').exec;

const showNotification = () => {
    const command = `osascript -e 'display notification "Tom! Is this working?"'`;
    exec(command, (error) => {});
};

command('danger', 'Only Tom should run this', showNotification);
