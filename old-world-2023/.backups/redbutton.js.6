item("home", "redbutton", {
    description: "A big red button"
});

itemCommand("press", "redbutton", (rest, player, item, game) => {
    for (let i = 0; i < 10; i += 1) {
        window.open('javascript:const el = document.createElement("p");el.textContent = "lol";document.body.appendChild(el)')
    }
});